# bin/build-plugin.sh stages the payload here; the release rsyncs from it into SVN
/build
/content-relations.zip

# wp-env
/.wp-env.override.json

# committed by accident once; a runtime log has no business in the repo
/resource/debug.log

.DS_Store
Thumbs.db
Icon

# compiled editor bundle - the pipeline builds it
/public/dist
/node_modules
