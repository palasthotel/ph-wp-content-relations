{
  "name": "content-relations",
  "private": true,
  "description": "Build tooling for the Content Relations block editor sidebar. The plugin itself is plain PHP; only the editor bundles are compiled.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "start": "wp-scripts start src/block-editor.js src/meta-box.js --output-path=public/dist",
    "build": "wp-scripts build src/block-editor.js src/meta-box.js --output-path=public/dist",
    "lint:js": "wp-scripts lint-js src"
  },
  "devDependencies": {
    "@wordpress/api-fetch": "^7.0.0",
    "@wordpress/components": "^28.0.0",
    "@wordpress/compose": "^7.0.0",
    "@wordpress/data": "^10.0.0",
    "@wordpress/editor": "^14.0.0",
    "@wordpress/element": "^6.0.0",
    "@wordpress/i18n": "^5.0.0",
    "@wordpress/icons": "^10.0.0",
    "@wordpress/scripts": "^30.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
